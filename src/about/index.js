import React from 'react';
import {render} from 'react-dom';
import '../style.css';

const Index = () => {

    return (
        <h1>About</h1>
    );
};

render(<Index/>, document.getElementById('target'));
