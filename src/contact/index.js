import React from 'react';
import {render} from 'react-dom';
import '../style.css';

const Index = () => {

    return (
        <h1>Contact</h1>
    );
};

render(<Index/>, document.getElementById('target'));
